const footerStyles = {
    background: '#071629',
    height: '249px',
}

const Footer = () => {
    return (
        <section style={footerStyles}>

        </section>
    )
}

export default Footer;